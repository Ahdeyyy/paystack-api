{
  "name": "paystack-api",
  "main": "dist/index.js",
  "description": "an api wrapper for paystack's api with types",
  "build": "bun build ./src/index.ts --outdir ./dist",
  "license": "GPL-3",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "tsup": "^8.0.2"
  }
}
